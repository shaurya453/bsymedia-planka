diff --git a/client/src/components/cards/CardModal/ProjectContent.jsx b/client/src/components/cards/CardModal/ProjectContent.jsx
index ce8145e..27431d1 100644
--- a/client/src/components/cards/CardModal/ProjectContent.jsx
+++ b/client/src/components/cards/CardModal/ProjectContent.jsx
@@ -35,6 +35,7 @@ import AssignedMembers from './AssignedMembers';
 import LabelChip from '../../labels/LabelChip';
 import LabelsStep from '../../labels/LabelsStep';
 import Attachments from '../../attachments/Attachments';
+import AddAttachmentStep from '../../attachments/AddAttachmentStep';
 
 import styles from './ProjectContent.module.scss';
 
@@ -84,6 +85,7 @@ const ProjectContent = React.memo(() => {
     canDelete,
     canUseMembers,
     canUseLabels,
+    canUseAttachments,
   } = useSelector((state) => {
     const boardMembership = selectors.selectCurrentUserMembershipForCurrentBoard(state);
 
@@ -110,6 +112,7 @@ const ProjectContent = React.memo(() => {
         canDelete: isEditor,
         canUseMembers: false,
         canUseLabels: false,
+        canUseAttachments: false,
       };
     }
 
@@ -127,6 +130,7 @@ const ProjectContent = React.memo(() => {
       canDelete: isEditor,
       canUseMembers: isEditor,
       canUseLabels: isEditor,
+      canUseAttachments: isEditor,
     };
   }, shallowEqual);
 
@@ -254,6 +258,7 @@ const ProjectContent = React.memo(() => {
   const CreationDetailsPopup = usePopupInClosableContext(CreationDetailsStep);
   const BoardMembershipsPopup = usePopupInClosableContext(BoardMembershipsStep);
   const LabelsPopup = usePopupInClosableContext(LabelsStep);
+  const AddAttachmentPopup = usePopupInClosableContext(AddAttachmentStep);
   const EditStopwatchPopup = usePopupInClosableContext(EditStopwatchStep);
   const MoreActionsPopup = usePopupInClosableContext(MoreActionsStep);
   const ConfirmationPopup = usePopupInClosableContext(ConfirmationStep);
@@ -612,6 +617,17 @@ const ProjectContent = React.memo(() => {
                 </LabelsPopup>
               </div>
             )}
+            {canUseAttachments && (
+              <div className={styles.actions}>
+                <span className={styles.actionsTitle}>{t('common.attachments')}</span>
+                <AddAttachmentPopup>
+                  <Button fluid className={classNames(styles.actionButton, styles.hidable)}>
+                    <Icon name="attach" className={styles.actionIcon} />
+                    {t('common.attachment')}
+                  </Button>
+                </AddAttachmentPopup>
+              </div>
+            )}
           </div>
         </Grid.Column>
       </Grid.Row>
diff --git a/client/src/components/cards/CardModal/StoryContent.jsx b/client/src/components/cards/CardModal/StoryContent.jsx
index 9cf5135..3534e6e 100644
--- a/client/src/components/cards/CardModal/StoryContent.jsx
+++ b/client/src/components/cards/CardModal/StoryContent.jsx
@@ -34,6 +34,7 @@ import AssignedMembers from './AssignedMembers';
 import LabelChip from '../../labels/LabelChip';
 import LabelsStep from '../../labels/LabelsStep';
 import Attachments from '../../attachments/Attachments';
+import AddAttachmentStep from '../../attachments/AddAttachmentStep';
 
 import styles from './StoryContent.module.scss';
 
@@ -90,6 +91,7 @@ const StoryContent = React.memo(() => {
     canDelete,
     canUseMembers,
     canUseLabels,
+    canUseAttachments,
   } = useSelector((state) => {
     const boardMembership = selectors.selectCurrentUserMembershipForCurrentBoard(state);
 
@@ -115,6 +117,7 @@ const StoryContent = React.memo(() => {
         canDelete: isEditor,
         canUseMembers: false,
         canUseLabels: false,
+        canUseAttachments: false,
       };
     }
 
@@ -131,6 +134,7 @@ const StoryContent = React.memo(() => {
       canDelete: isEditor,
       canUseMembers: isEditor,
       canUseLabels: isEditor,
+      canUseAttachments: isEditor,
     };
   }, shallowEqual);
 
@@ -259,6 +263,7 @@ const StoryContent = React.memo(() => {
   const CreationDetailsPopup = usePopupInClosableContext(CreationDetailsStep);
   const BoardMembershipsPopup = usePopupInClosableContext(BoardMembershipsStep);
   const LabelsPopup = usePopupInClosableContext(LabelsStep);
+  const AddAttachmentPopup = usePopupInClosableContext(AddAttachmentStep);
   const MoreActionsPopup = usePopupInClosableContext(MoreActionsStep);
   const ConfirmationPopup = usePopupInClosableContext(ConfirmationStep);
 
@@ -593,6 +598,17 @@ const StoryContent = React.memo(() => {
                 </LabelsPopup>
               </div>
             )}
+            {canUseAttachments && (
+              <div className={styles.actions}>
+                <span className={styles.actionsTitle}>{t('common.attachments')}</span>
+                <AddAttachmentPopup>
+                  <Button fluid className={classNames(styles.actionButton, styles.hidable)}>
+                    <Icon name="attach" className={styles.actionIcon} />
+                    {t('common.attachment')}
+                  </Button>
+                </AddAttachmentPopup>
+              </div>
+            )}
           </div>
         </Grid.Column>
       </Grid.Row>
diff --git a/client/src/components/common/Toaster/ListArchiveFailedToast.jsx b/client/src/components/common/Toaster/ListArchiveFailedToast.jsx
new file mode 100644
index 0000000..138869d
--- /dev/null
+++ b/client/src/components/common/Toaster/ListArchiveFailedToast.jsx
@@ -0,0 +1,21 @@
+/*!
+ * Copyright (c) 2024 PLANKA Software GmbH
+ * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
+ */
+
+import React from 'react';
+import { useTranslation } from 'react-i18next';
+import { Icon, Message } from 'semantic-ui-react';
+
+const ListArchiveFailedToast = React.memo(() => {
+  const [t] = useTranslation();
+
+  return (
+    <Message visible negative size="tiny">
+      <Icon name="archive" />
+      {t('common.listArchiveFailed')}
+    </Message>
+  );
+});
+
+export default ListArchiveFailedToast;
diff --git a/client/src/components/common/Toaster/Toaster.jsx b/client/src/components/common/Toaster/Toaster.jsx
index 0b53c3f..e3ad61e 100644
--- a/client/src/components/common/Toaster/Toaster.jsx
+++ b/client/src/components/common/Toaster/Toaster.jsx
@@ -12,6 +12,7 @@ import NotEnoughStorageToast from './NotEnoughStorageToast';
 import EmptyTrashToast from './EmptyTrashToast';
 import SourceCardNotCopyableToast from './SourceCardNotCopyableToast';
 import SourceCardNotMovableToast from './SourceCardNotMovableToast';
+import ListArchiveFailedToast from './ListArchiveFailedToast';
 
 const TOAST_BY_TYPE = {
   [ToastTypes.FILE_IS_TOO_BIG]: FileIsTooBigToast,
@@ -19,6 +20,7 @@ const TOAST_BY_TYPE = {
   [ToastTypes.EMPTY_TRASH]: EmptyTrashToast,
   [ToastTypes.SOURCE_CARD_NOT_COPYABLE]: SourceCardNotCopyableToast,
   [ToastTypes.SOURCE_CARD_NOT_MOVABLE]: SourceCardNotMovableToast,
+  [ToastTypes.LIST_ARCHIVE_FAILED]: ListArchiveFailedToast,
 };
 
 const Toaster = React.memo(() => (
diff --git a/client/src/constants/ToastTypes.js b/client/src/constants/ToastTypes.js
index 9af16ae..dccc321 100644
--- a/client/src/constants/ToastTypes.js
+++ b/client/src/constants/ToastTypes.js
@@ -8,6 +8,7 @@ const NOT_ENOUGH_STORAGE = 'NOT_ENOUGH_STORAGE';
 const EMPTY_TRASH = 'EMPTY_TRASH';
 const SOURCE_CARD_NOT_COPYABLE = 'SOURCE_CARD_NOT_COPYABLE';
 const SOURCE_CARD_NOT_MOVABLE = 'SOURCE_CARD_NOT_MOVABLE';
+const LIST_ARCHIVE_FAILED = 'LIST_ARCHIVE_FAILED';
 
 export default {
   FILE_IS_TOO_BIG,
@@ -15,4 +16,5 @@ export default {
   EMPTY_TRASH,
   SOURCE_CARD_NOT_COPYABLE,
   SOURCE_CARD_NOT_MOVABLE,
+  LIST_ARCHIVE_FAILED,
 };
diff --git a/client/src/locales/en-US/core.js b/client/src/locales/en-US/core.js
index 15d4b4a..601952d 100644
--- a/client/src/locales/en-US/core.js
+++ b/client/src/locales/en-US/core.js
@@ -247,6 +247,7 @@ export default {
       linkToCard: 'Link to card',
       list: 'List',
       listActions_title: 'List Actions',
+      listArchiveFailed: 'Failed to archive list. Please try again.',
       lists: 'Lists',
       makeProjectPrivate_title: 'Make Project Private',
       makeProjectShared_title: 'Make Project Shared',
diff --git a/client/src/models/List.js b/client/src/models/List.js
index 72ab1ec..c22cf42 100755
--- a/client/src/models/List.js
+++ b/client/src/models/List.js
@@ -137,6 +137,20 @@ export default class extends BaseModel {
         });
 
         break;
+      case ActionTypes.LIST_ARCHIVE__FAILURE: {
+        // BSY Media: roll back the optimistic hide above if the request
+        // actually failed - otherwise the list (and its cards) stays
+        // invisible indefinitely with no indication anything went wrong.
+        const list = List.withId(payload.id);
+
+        if (list) {
+          list.update({
+            archivedAt: null,
+          });
+        }
+
+        break;
+      }
       case ActionTypes.LIST_CREATE__SUCCESS:
         List.withId(payload.localId).delete();
         List.upsert(prepareList(payload.list));
diff --git a/client/src/sagas/core/services/lists.js b/client/src/sagas/core/services/lists.js
index bb23d16..49284a7 100644
--- a/client/src/sagas/core/services/lists.js
+++ b/client/src/sagas/core/services/lists.js
@@ -245,6 +245,14 @@ export function* archiveList(id) {
     ({ item: list } = yield call(request, api.archiveList, id));
   } catch (error) {
     yield put(actions.archiveList.failure(id, error));
+
+    // BSY Media: the model reducer's LIST_ARCHIVE__FAILURE case already
+    // un-hides the list; this just tells the user why it reappeared,
+    // instead of leaving them to notice a silent revert.
+    yield call(toast, {
+      type: ToastTypes.LIST_ARCHIVE_FAILED,
+    });
+
     return;
   }
 
