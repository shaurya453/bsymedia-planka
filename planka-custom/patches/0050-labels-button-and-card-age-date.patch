diff --git a/client/src/components/cards/Card/ProjectContent.jsx b/client/src/components/cards/Card/ProjectContent.jsx
index 3d8b5ff..aec458f 100644
--- a/client/src/components/cards/Card/ProjectContent.jsx
+++ b/client/src/components/cards/Card/ProjectContent.jsx
@@ -7,6 +7,7 @@ import React, { useCallback, useMemo } from 'react';
 import PropTypes from 'prop-types';
 import classNames from 'classnames';
 import { shallowEqual, useDispatch, useSelector } from 'react-redux';
+import { useTranslation } from 'react-i18next';
 import { Icon } from 'semantic-ui-react';
 
 import selectors from '../../../selectors';
@@ -14,6 +15,7 @@ import entryActions from '../../../entry-actions';
 import { startStopwatch, stopStopwatch } from '../../../utils/stopwatch';
 import { isListArchiveOrTrash } from '../../../utils/record-helpers';
 import moveCurrentUserFirst from '../../../utils/move-current-user-first';
+import getDateFormat from '../../../utils/get-date-format';
 import { BoardMembershipRoles, BoardViews } from '../../../constants/Enums';
 import TaskList from './TaskList';
 import StopwatchChip from '../StopwatchChip';
@@ -107,6 +109,7 @@ const ProjectContent = React.memo(({ cardId }) => {
   });
 
   const dispatch = useDispatch();
+  const [t] = useTranslation();
 
   const handleToggleStopwatchClick = useCallback(
     (event) => {
@@ -250,9 +253,21 @@ const ProjectContent = React.memo(({ cardId }) => {
           )}
           {withAge && card.createdAt && (
             <span className={classNames(styles.attachment, styles.attachmentLeft)}>
-              <span className={styles.attachmentContent}>
+              <span className={classNames(styles.attachmentContent, styles.ageAttachmentContent)}>
                 <Icon name="history" />
                 <TimeAgo date={card.createdAt} />
+                {/* BSY Media: shows the absolute creation date underneath the
+                    relative "x minutes ago" text, so it's visible at a glance
+                    instead of only on hover (TimeAgo already sets that as a
+                    title tooltip). Reuses the same fullDate/longDate i18n
+                    format keys and same-year heuristic as verbose date
+                    tooltips elsewhere in the app. */}
+                <span className={styles.ageDate}>
+                  {t(`format:${getDateFormat(card.createdAt, 'longDate', 'fullDate')}`, {
+                    value: card.createdAt,
+                    postProcess: 'formatDate',
+                  })}
+                </span>
               </span>
             </span>
           )}
diff --git a/client/src/components/cards/Card/ProjectContent.module.scss b/client/src/components/cards/Card/ProjectContent.module.scss
index 9c47e5c..052f3f5 100644
--- a/client/src/components/cards/Card/ProjectContent.module.scss
+++ b/client/src/components/cards/Card/ProjectContent.module.scss
@@ -25,6 +25,21 @@
     white-space: nowrap;
   }
 
+  .ageAttachmentContent {
+    line-height: 14px;
+    overflow: visible;
+    padding-bottom: 2px;
+    padding-top: 2px;
+  }
+
+  .ageDate {
+    color: #9fadb3;
+    display: block;
+    font-size: 10px;
+    line-height: 14px;
+    margin-left: 16px;
+  }
+
   .attachmentLeft {
     margin-right: 4px;
   }
diff --git a/client/src/components/cards/Card/StoryContent.jsx b/client/src/components/cards/Card/StoryContent.jsx
index b5ec8f3..218dc5f 100644
--- a/client/src/components/cards/Card/StoryContent.jsx
+++ b/client/src/components/cards/Card/StoryContent.jsx
@@ -7,10 +7,12 @@ import React, { useMemo } from 'react';
 import PropTypes from 'prop-types';
 import classNames from 'classnames';
 import { shallowEqual, useSelector } from 'react-redux';
+import { useTranslation } from 'react-i18next';
 import { Icon } from 'semantic-ui-react';
 
 import selectors from '../../../selectors';
 import markdownToText from '../../../utils/markdown-to-text';
+import getDateFormat from '../../../utils/get-date-format';
 import { BoardViews } from '../../../constants/Enums';
 import TaskList from './TaskList';
 import TimeAgo from '../../common/TimeAgo';
@@ -51,6 +53,8 @@ const StoryContent = React.memo(({ cardId }) => {
 
   const selectAttachmentById = useMemo(() => selectors.makeSelectAttachmentById(), []);
 
+  const [t] = useTranslation();
+
   const card = useSelector((state) => selectCardById(state, cardId));
   const list = useSelector((state) => selectListById(state, card.listId));
   const labelIds = useSelector((state) => selectLabelIdsByCardId(state, cardId));
@@ -176,9 +180,17 @@ const StoryContent = React.memo(({ cardId }) => {
             )}
             {withAge && card.createdAt && (
               <span className={classNames(styles.attachment, styles.attachmentLeft)}>
-                <span className={styles.attachmentContent}>
+                <span
+                  className={classNames(styles.attachmentContent, styles.ageAttachmentContent)}
+                >
                   <Icon name="history" />
                   <TimeAgo date={card.createdAt} />
+                  <span className={styles.ageDate}>
+                    {t(`format:${getDateFormat(card.createdAt, 'longDate', 'fullDate')}`, {
+                      value: card.createdAt,
+                      postProcess: 'formatDate',
+                    })}
+                  </span>
                 </span>
               </span>
             )}
diff --git a/client/src/components/cards/Card/StoryContent.module.scss b/client/src/components/cards/Card/StoryContent.module.scss
index ad72b18..b47e1aa 100644
--- a/client/src/components/cards/Card/StoryContent.module.scss
+++ b/client/src/components/cards/Card/StoryContent.module.scss
@@ -25,6 +25,21 @@
     white-space: nowrap;
   }
 
+  .ageAttachmentContent {
+    line-height: 14px;
+    overflow: visible;
+    padding-bottom: 2px;
+    padding-top: 2px;
+  }
+
+  .ageDate {
+    color: #9fadb3;
+    display: block;
+    font-size: 10px;
+    line-height: 14px;
+    margin-left: 16px;
+  }
+
   .attachmentLeft {
     margin-right: 4px;
   }
diff --git a/client/src/components/cards/CardModal/ProjectContent.jsx b/client/src/components/cards/CardModal/ProjectContent.jsx
index f666fff..ce8145e 100644
--- a/client/src/components/cards/CardModal/ProjectContent.jsx
+++ b/client/src/components/cards/CardModal/ProjectContent.jsx
@@ -596,6 +596,22 @@ const ProjectContent = React.memo(() => {
                 )}
               </div>
             )}
+            {canUseLabels && (
+              <div className={styles.actions}>
+                <span className={styles.actionsTitle}>{t('common.labels')}</span>
+                <LabelsPopup
+                  currentIds={labelIds}
+                  cardId={card.id}
+                  onSelect={handleLabelSelect}
+                  onDeselect={handleLabelDeselect}
+                >
+                  <Button fluid className={classNames(styles.actionButton, styles.hidable)}>
+                    <Icon name="tags" className={styles.actionIcon} />
+                    {t('common.labels')}
+                  </Button>
+                </LabelsPopup>
+              </div>
+            )}
           </div>
         </Grid.Column>
       </Grid.Row>
diff --git a/client/src/components/cards/CardModal/StoryContent.jsx b/client/src/components/cards/CardModal/StoryContent.jsx
index 8019328..9cf5135 100644
--- a/client/src/components/cards/CardModal/StoryContent.jsx
+++ b/client/src/components/cards/CardModal/StoryContent.jsx
@@ -577,6 +577,22 @@ const StoryContent = React.memo(() => {
                 )}
               </div>
             )}
+            {canUseLabels && (
+              <div className={styles.actions}>
+                <span className={styles.actionsTitle}>{t('common.labels')}</span>
+                <LabelsPopup
+                  currentIds={labelIds}
+                  cardId={card.id}
+                  onSelect={handleLabelSelect}
+                  onDeselect={handleLabelDeselect}
+                >
+                  <Button fluid className={classNames(styles.actionButton, styles.hidable)}>
+                    <Icon name="tags" className={styles.actionIcon} />
+                    {t('common.labels')}
+                  </Button>
+                </LabelsPopup>
+              </div>
+            )}
           </div>
         </Grid.Column>
       </Grid.Row>
